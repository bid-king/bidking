pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls'
                sh 'cd server && ls'
                echo 'Hello World'
            }
        }
    }
}